; Settings for guzzle security.
; Use the system bundle, which update-ca-certificates builds from the public
; CAs plus the moodle-docker root CA (see Dockerfile) - so PHP can verify both
; public sites and every instance's local services (webserver2, keycloak2, ...).
openssl.cafile=/etc/ssl/certs/ca-certificates.crt
